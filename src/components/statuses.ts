export enum AppRoute {
    Login = '/login',
    Main = '/',
    Favorites = '/favorites',
    Room = '/offer'
}

export enum AuthStatus {
    Auth = 'AUTH',
    NoAuth = 'NO_AUTH',
    Unknown = 'UNKNOWN'
}
